services:
  - type: web
    name: dovidka-app
    env: python
    buildCommand: "pip install -r requirements.txt && apt-get install -y fonts-dejavu-core"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
